{
  "version": 2,
  "buildCommand": "cd apexad-website && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "apexad-website/build",
  "installCommand": "cd apexad-website && npm install --legacy-peer-deps",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
} 